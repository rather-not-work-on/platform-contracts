# schemas

## Responsibility
Own the canonical JSON Schema definitions consumed across the federation.

## Contents
- `c1` through `c8` schema files
- publication/pin evidence schemas

## Rules
- one schema per file using `<name>.schema.json`
- no fixtures, reports, or prose documents
- compatibility decisions belong in `compatibility/`, not inline ad hoc notes
