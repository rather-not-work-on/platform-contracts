# compatibility

## Responsibility
Store stable human-readable policy and runbooks for versioning and bundle publication.

## Contents
- SemVer policy
- schema strictness policy
- publish-and-pin runbook
- consumer pin map

## Rules
- generated JSON reports belong under `compatibility/reports/` and stay gitignored
- executable validation logic belongs in `scripts/`
- canonical contract files remain in `schemas/`
